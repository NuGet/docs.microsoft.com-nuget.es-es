---
title: Proveedores de credenciales de NuGet.exe
description: proveedores de credenciales de NuGet.exe autentican con una fuente de distribución y se implementan como archivos ejecutables de línea de comandos que siguen las convenciones específicas.
author: karann-msft
ms.author: karann
ms.date: 12/12/2017
ms.topic: conceptual
ms.openlocfilehash: 97a44c6d561f426fa50fa068a9bbf793f77a3111
ms.sourcegitcommit: 1d1406764c6af5fb7801d462e0c4afc9092fa569
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/04/2018
ms.locfileid: "43550194"
---
# <a name="authenticating-feeds-with-nugetexe-credential-providers"></a><span data-ttu-id="dab8b-103">Autenticar fuentes con proveedores de credenciales de nuget.exe</span><span class="sxs-lookup"><span data-stu-id="dab8b-103">Authenticating feeds with nuget.exe credential providers</span></span>

<span data-ttu-id="dab8b-104">*NuGet 3.3 +*</span><span class="sxs-lookup"><span data-stu-id="dab8b-104">*NuGet 3.3+*</span></span>

<span data-ttu-id="dab8b-105">Cuando `nuget.exe` necesita credenciales para autenticarse con una fuente, lo busca en la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="dab8b-105">When `nuget.exe` needs credentials to authenticate with a feed, it looks for them in the following manner:</span></span>

1. <span data-ttu-id="dab8b-106">NuGet busca primero las credenciales de `Nuget.Config` archivos.</span><span class="sxs-lookup"><span data-stu-id="dab8b-106">NuGet first looks for credentials in `Nuget.Config` files.</span></span>
1. <span data-ttu-id="dab8b-107">NuGet, a continuación, usa los proveedores de credenciales de complemento, sujeto a la orden que se indican a continuación.</span><span class="sxs-lookup"><span data-stu-id="dab8b-107">NuGet then uses plug-in credential providers, subject to the order given below.</span></span> <span data-ttu-id="dab8b-108">(Y de ejemplo es el [Visual Studio Team Services credencial Provider](https://www.visualstudio.com/docs/package/get-started/nuget/auth#vsts-credential-provider).)</span><span class="sxs-lookup"><span data-stu-id="dab8b-108">(And example is the [Visual Studio Team Services Credential Provider](https://www.visualstudio.com/docs/package/get-started/nuget/auth#vsts-credential-provider).)</span></span>
1. <span data-ttu-id="dab8b-109">NuGet, a continuación, pide al usuario las credenciales en la línea de comandos.</span><span class="sxs-lookup"><span data-stu-id="dab8b-109">NuGet then prompts the user for credentials on the command line.</span></span>

<span data-ttu-id="dab8b-110">Tenga en cuenta que los proveedores de credenciales se describe aquí funcionan únicamente en `nuget.exe` y no en Visual Studio o de "dotnet restore".</span><span class="sxs-lookup"><span data-stu-id="dab8b-110">Note that the credential providers described here work only in `nuget.exe` and not in 'dotnet restore' or Visual Studio.</span></span> <span data-ttu-id="dab8b-111">Para los proveedores de credenciales con Visual Studio, consulte [nuget.exe proveedores de credenciales para Visual Studio](nuget-credential-providers-for-visual-studio.md)</span><span class="sxs-lookup"><span data-stu-id="dab8b-111">For credential providers with Visual Studio, see [nuget.exe Credential Providers for Visual Studio](nuget-credential-providers-for-visual-studio.md)</span></span>

<span data-ttu-id="dab8b-112">los proveedores de credenciales de NuGet.exe se pueden usar de 3 maneras:</span><span class="sxs-lookup"><span data-stu-id="dab8b-112">nuget.exe credential providers can be used in 3 ways:</span></span>

- <span data-ttu-id="dab8b-113">**Globalmente**: para que un proveedor de credenciales disponibles en todas las instancias de `nuget.exe` se ejecutan en el perfil del usuario actual, agréguela a `%LocalAppData%\NuGet\CredentialProviders`.</span><span class="sxs-lookup"><span data-stu-id="dab8b-113">**Globally**: to make a credential provider available to all instances of `nuget.exe` run under the current user's profile, add it to `%LocalAppData%\NuGet\CredentialProviders`.</span></span> <span data-ttu-id="dab8b-114">Es posible que deba crear la `CredentialProviders` carpeta.</span><span class="sxs-lookup"><span data-stu-id="dab8b-114">You may need to create the `CredentialProviders` folder.</span></span> <span data-ttu-id="dab8b-115">Se pueden instalar proveedores de credenciales en la raíz de la `CredentialProviders` carpeta o dentro de una subcarpeta.</span><span class="sxs-lookup"><span data-stu-id="dab8b-115">Credential providers can be installed at the root of the `CredentialProviders`  folder or within a subfolder.</span></span> <span data-ttu-id="dab8b-116">Si un proveedor de credenciales tiene varios archivos y ensamblados, puede usar las subcarpetas para mantener los proveedores que se organizan.</span><span class="sxs-lookup"><span data-stu-id="dab8b-116">If a credential provider has multiple files/assemblies, you can use subfolders to keep the providers organized.</span></span>

- <span data-ttu-id="dab8b-117">**Desde una variable de entorno**: proveedores de credenciales se pueden almacenar en cualquier lugar y accesibles para `nuget.exe` estableciendo el `%NUGET_CREDENTIALPROVIDERS_PATH%` variable de entorno para la ubicación del proveedor.</span><span class="sxs-lookup"><span data-stu-id="dab8b-117">**From an environment variable**: Credential providers can be stored anywhere and made accessible to `nuget.exe` by setting the `%NUGET_CREDENTIALPROVIDERS_PATH%` environment variable to the provider location.</span></span> <span data-ttu-id="dab8b-118">Esta variable puede ser una lista separada por punto y coma (por ejemplo, `path1;path2`) si tiene varias ubicaciones.</span><span class="sxs-lookup"><span data-stu-id="dab8b-118">This variable can be a semicolon-separated list (for example, `path1;path2`) if you have multiple locations.</span></span>

- <span data-ttu-id="dab8b-119">**Junto con nuget.exe**: proveedores de credenciales de nuget.exe se pueden colocar en la misma carpeta que `nuget.exe`.</span><span class="sxs-lookup"><span data-stu-id="dab8b-119">**Alongside nuget.exe**: nuget.exe credential providers can be placed in the same folder as `nuget.exe`.</span></span>

<span data-ttu-id="dab8b-120">Al cargar los proveedores de credenciales, `nuget.exe` busca en las ubicaciones anteriores, en orden, para cualquier archivo denominado `credentialprovider*.exe`, a continuación, carga los archivos en el orden en que se encuentran.</span><span class="sxs-lookup"><span data-stu-id="dab8b-120">When loading credential providers, `nuget.exe` searches the above locations, in order, for any file named `credentialprovider*.exe`, then loads those files in the order they're found.</span></span> <span data-ttu-id="dab8b-121">Si existen varios proveedores de credenciales en la misma carpeta, están cargados en orden alfabético.</span><span class="sxs-lookup"><span data-stu-id="dab8b-121">If multiple credential providers exist in the same folder, they're loaded in alphabetical order.</span></span>

## <a name="creating-a-nugetexe-credential-provider"></a><span data-ttu-id="dab8b-122">Creación de un proveedor de credenciales de nuget.exe</span><span class="sxs-lookup"><span data-stu-id="dab8b-122">Creating a nuget.exe credential provider</span></span>

<span data-ttu-id="dab8b-123">Un proveedor de credenciales es un ejecutable de línea de comandos, con el formato `CredentialProvider*.exe`, que recopila entradas, adquiere las credenciales según corresponda y, a continuación, devuelve el código de estado de salida adecuada y la salida estándar.</span><span class="sxs-lookup"><span data-stu-id="dab8b-123">A credential provider is a command-line executable, named in the form `CredentialProvider*.exe`, that gathers inputs, acquires credentials as appropriate, and then returns the appropriate exit status code and standard output.</span></span>

<span data-ttu-id="dab8b-124">Un proveedor debe hacer lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="dab8b-124">A provider must do the following:</span></span>

- <span data-ttu-id="dab8b-125">Determinar si puede proporcionar credenciales para el URI de destino antes de iniciar la adquisición de credenciales.</span><span class="sxs-lookup"><span data-stu-id="dab8b-125">Determine whether it can provide credentials for the targeted URI before initiating credential acquisition.</span></span> <span data-ttu-id="dab8b-126">Si no es así, debe devolver el código de estado 1 sin credenciales.</span><span class="sxs-lookup"><span data-stu-id="dab8b-126">If not, it should return status code 1 with no credentials.</span></span>
- <span data-ttu-id="dab8b-127">No modifique `Nuget.Config` (por ejemplo, para establecer las credenciales no existe).</span><span class="sxs-lookup"><span data-stu-id="dab8b-127">Not modify `Nuget.Config` (such as setting credentials there).</span></span>
- <span data-ttu-id="dab8b-128">Configuración de proxy de controlador HTTP en su propio, como NuGet no proporciona información de proxy para el complemento.</span><span class="sxs-lookup"><span data-stu-id="dab8b-128">Handle HTTP proxy configuration on its own, as NuGet does not provide proxy information to the plugin.</span></span>
- <span data-ttu-id="dab8b-129">Devolver las credenciales o los detalles del error a `nuget.exe` escribiendo un objeto de respuesta JSON (ver abajo) en stdout, utilizando la codificación UTF-8.</span><span class="sxs-lookup"><span data-stu-id="dab8b-129">Return credentials or error details to `nuget.exe` by writing a JSON response object (see below) to stdout, using UTF-8 encoding.</span></span>
- <span data-ttu-id="dab8b-130">Emitir registros de seguimiento adicionales en stderr.</span><span class="sxs-lookup"><span data-stu-id="dab8b-130">Optionally emit additional trace logging to stderr.</span></span> <span data-ttu-id="dab8b-131">Nunca se debe escribir ningún secreto en stderr, puesto que en los niveles de detalle "normales" o "detallados" se reflejan los seguimientos mediante NuGet en la consola.</span><span class="sxs-lookup"><span data-stu-id="dab8b-131">No secrets should ever be written to stderr, since at verbosity levels "normal" or "detailed" such traces are echoed by NuGet to the console.</span></span>
- <span data-ttu-id="dab8b-132">Parámetros inesperados se deben omitir; que proporciona compatibilidad con versiones posteriores con futuras versiones de NuGet.</span><span class="sxs-lookup"><span data-stu-id="dab8b-132">Unexpected parameters should be ignored, providing forward compatibility with future versions of NuGet.</span></span>

### <a name="input-parameters"></a><span data-ttu-id="dab8b-133">Parámetros de entrada</span><span class="sxs-lookup"><span data-stu-id="dab8b-133">Input parameters</span></span>

| <span data-ttu-id="dab8b-134">Parámetro o modificador</span><span class="sxs-lookup"><span data-stu-id="dab8b-134">Parameter/Switch</span></span> |<span data-ttu-id="dab8b-135">Descripción</span><span class="sxs-lookup"><span data-stu-id="dab8b-135">Description</span></span>|
|----------------|-----------|
| <span data-ttu-id="dab8b-136">URI {value}</span><span class="sxs-lookup"><span data-stu-id="dab8b-136">Uri {value}</span></span> | <span data-ttu-id="dab8b-137">El paquete de origen que requieren credenciales de URI.</span><span class="sxs-lookup"><span data-stu-id="dab8b-137">The package source URI requiring credentials.</span></span>|
| <span data-ttu-id="dab8b-138">No interactivo</span><span class="sxs-lookup"><span data-stu-id="dab8b-138">NonInteractive</span></span> | <span data-ttu-id="dab8b-139">Si está presente, el proveedor no emite solicitudes interactivas.</span><span class="sxs-lookup"><span data-stu-id="dab8b-139">If present, provider does not issue interactive prompts.</span></span> |
| <span data-ttu-id="dab8b-140">isRetry</span><span class="sxs-lookup"><span data-stu-id="dab8b-140">IsRetry</span></span> | <span data-ttu-id="dab8b-141">Si está presente, indica que este intento es un reintento de un intento fallido anteriormente.</span><span class="sxs-lookup"><span data-stu-id="dab8b-141">If present, indicates that this attempt is a retry of a previously failed attempt.</span></span> <span data-ttu-id="dab8b-142">Los proveedores suelen utilizar esta marca para asegurarse de que omitir cualquier caché existente y solicitará las credenciales de nuevo si es posible.</span><span class="sxs-lookup"><span data-stu-id="dab8b-142">Providers typically use this flag to ensure that they bypass any existing cache and prompt for new credentials if possible.</span></span>|
| <span data-ttu-id="dab8b-143">Nivel de detalle {value}</span><span class="sxs-lookup"><span data-stu-id="dab8b-143">Verbosity {value}</span></span> | <span data-ttu-id="dab8b-144">Si está presente, uno de los siguientes valores: "normal", "quiet" o "detallado".</span><span class="sxs-lookup"><span data-stu-id="dab8b-144">If present, one of the following values: "normal", "quiet", or "detailed".</span></span> <span data-ttu-id="dab8b-145">Si se proporciona ningún valor, valor predeterminado es "normal".</span><span class="sxs-lookup"><span data-stu-id="dab8b-145">If no value is supplied, defaults to "normal".</span></span> <span data-ttu-id="dab8b-146">Proveedores deben usar esto como una indicación del nivel de registro opcional para emitir en el flujo de error estándar.</span><span class="sxs-lookup"><span data-stu-id="dab8b-146">Providers should use this as an indication of the level of optional logging to emit to the standard error stream.</span></span> |

### <a name="exit-codes"></a><span data-ttu-id="dab8b-147">Códigos de salida</span><span class="sxs-lookup"><span data-stu-id="dab8b-147">Exit codes</span></span>

| <span data-ttu-id="dab8b-148">Código</span><span class="sxs-lookup"><span data-stu-id="dab8b-148">Code</span></span> |<span data-ttu-id="dab8b-149">Resultado</span><span class="sxs-lookup"><span data-stu-id="dab8b-149">Result</span></span> | <span data-ttu-id="dab8b-150">Descripción</span><span class="sxs-lookup"><span data-stu-id="dab8b-150">Description</span></span> |
|----------------|-----------|-----------|
| <span data-ttu-id="dab8b-151">0</span><span class="sxs-lookup"><span data-stu-id="dab8b-151">0</span></span> | <span data-ttu-id="dab8b-152">Correcto</span><span class="sxs-lookup"><span data-stu-id="dab8b-152">Success</span></span> | <span data-ttu-id="dab8b-153">Las credenciales se han adquirido correctamente y se han escrito en stdout.</span><span class="sxs-lookup"><span data-stu-id="dab8b-153">Credentials were successfully acquired and have been written to stdout.</span></span>|
| <span data-ttu-id="dab8b-154">1</span><span class="sxs-lookup"><span data-stu-id="dab8b-154">1</span></span> | <span data-ttu-id="dab8b-155">ProviderNotApplicable</span><span class="sxs-lookup"><span data-stu-id="dab8b-155">ProviderNotApplicable</span></span> | <span data-ttu-id="dab8b-156">El proveedor actual no proporciona credenciales para el URI dado.</span><span class="sxs-lookup"><span data-stu-id="dab8b-156">The current provider does not provide credentials for the given URI.</span></span>|
| <span data-ttu-id="dab8b-157">2</span><span class="sxs-lookup"><span data-stu-id="dab8b-157">2</span></span> | <span data-ttu-id="dab8b-158">Error</span><span class="sxs-lookup"><span data-stu-id="dab8b-158">Failure</span></span> | <span data-ttu-id="dab8b-159">El proveedor es el proveedor correcto para el URI dado, pero no puede proporcionar las credenciales.</span><span class="sxs-lookup"><span data-stu-id="dab8b-159">The provider is the correct provider for the given URI, but cannot provide credentials.</span></span> <span data-ttu-id="dab8b-160">En este caso, nuget.exe no volverá a intentar la autenticación y se producirá un error.</span><span class="sxs-lookup"><span data-stu-id="dab8b-160">In this case, nuget.exe will not retry authentication and will fail.</span></span> <span data-ttu-id="dab8b-161">Un ejemplo típico es cuando un usuario cancela un inicio de sesión interactivo.</span><span class="sxs-lookup"><span data-stu-id="dab8b-161">A typical example is when a user cancels an interactive login.</span></span> |

### <a name="standard-output"></a><span data-ttu-id="dab8b-162">Salida estándar</span><span class="sxs-lookup"><span data-stu-id="dab8b-162">Standard output</span></span>

| <span data-ttu-id="dab8b-163">Property</span><span class="sxs-lookup"><span data-stu-id="dab8b-163">Property</span></span> |<span data-ttu-id="dab8b-164">Notas</span><span class="sxs-lookup"><span data-stu-id="dab8b-164">Notes</span></span>|
|----------------|-----------|
| <span data-ttu-id="dab8b-165">Nombre de usuario</span><span class="sxs-lookup"><span data-stu-id="dab8b-165">Username</span></span> | <span data-ttu-id="dab8b-166">Nombre de usuario para las solicitudes autenticadas.</span><span class="sxs-lookup"><span data-stu-id="dab8b-166">Username for authenticated requests.</span></span>|
| <span data-ttu-id="dab8b-167">Contraseña</span><span class="sxs-lookup"><span data-stu-id="dab8b-167">Password</span></span> | <span data-ttu-id="dab8b-168">Contraseña para las solicitudes autenticadas.</span><span class="sxs-lookup"><span data-stu-id="dab8b-168">Password for authenticated requests.</span></span>|
| <span data-ttu-id="dab8b-169">Mensaje</span><span class="sxs-lookup"><span data-stu-id="dab8b-169">Message</span></span> | <span data-ttu-id="dab8b-170">Detalles opcionales acerca de la respuesta, que se usa solo para mostrar detalles adicionales en caso de error.</span><span class="sxs-lookup"><span data-stu-id="dab8b-170">Optional details about the response, used only to show additional details in failure cases.</span></span> |

<span data-ttu-id="dab8b-171">Stdout de ejemplo:</span><span class="sxs-lookup"><span data-stu-id="dab8b-171">Example stdout:</span></span>

    { "Username" : "freddy@example.com",
      "Password" : "bwm3bcx6txhprzmxhl2x63mdsul6grctazoomtdb6kfbof7m3a3z",
      "Message"  : "" }

## <a name="troubleshooting-a-credential-provider"></a><span data-ttu-id="dab8b-172">Solución de problemas de un proveedor de credenciales</span><span class="sxs-lookup"><span data-stu-id="dab8b-172">Troubleshooting a credential provider</span></span>

<span data-ttu-id="dab8b-173">En este momento, NuGet no proporciona mucha compatibilidad directa para la depuración de proveedores de credenciales personalizadas; [emitir 4598](https://github.com/NuGet/Home/issues/4598) está realizando el seguimiento este trabajo.</span><span class="sxs-lookup"><span data-stu-id="dab8b-173">At present, NuGet doesn't provide much direct support for debugging custom credential providers; [issue 4598](https://github.com/NuGet/Home/issues/4598) is tracking this work.</span></span>

<span data-ttu-id="dab8b-174">También puede hacer lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="dab8b-174">You can also do the following:</span></span>

- <span data-ttu-id="dab8b-175">Ejecute nuget.exe con el `-verbosity` conmutador para inspeccionar la salida detallada.</span><span class="sxs-lookup"><span data-stu-id="dab8b-175">Run nuget.exe with the `-verbosity` switch to inspect detailed output.</span></span>
- <span data-ttu-id="dab8b-176">Agregar mensajes de depuración al `stdout` en lugares adecuados.</span><span class="sxs-lookup"><span data-stu-id="dab8b-176">Add debug messages to `stdout` in appropriate places.</span></span>
- <span data-ttu-id="dab8b-177">Asegúrese de que está usando nuget.exe 3.3 o posterior.</span><span class="sxs-lookup"><span data-stu-id="dab8b-177">Be sure that you're using nuget.exe 3.3 or higher.</span></span>
- <span data-ttu-id="dab8b-178">Adjunte el depurador durante el inicio con este fragmento de código:</span><span class="sxs-lookup"><span data-stu-id="dab8b-178">Attach debugger on startup with this code snippet:</span></span>

    ```cs
    while (!Debugger.IsAttached)
    {
        System.Threading.Thread.Sleep(100);
    }
    Debugger.Break();
    ```

<span data-ttu-id="dab8b-179">Para obtener más ayuda, [enviar una solicitud de soporte técnico una nuget.org](https://www.nuget.org/policies/Contact).</span><span class="sxs-lookup"><span data-stu-id="dab8b-179">For further help, [submit a support request a nuget.org](https://www.nuget.org/policies/Contact).</span></span>
