---
title: Comando de instalación de la CLI de NuGet
description: Referencia para el comando de instalación de nuget.exe
author: karann-msft
ms.author: karann
manager: unnir
ms.date: 01/18/2018
ms.topic: reference
ms.openlocfilehash: e609b01bc14083ce212f6d4d4c6d3412f0ee316b
ms.sourcegitcommit: 4d139cb54a46616ae48d1768fa108ae3bf450d5b
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 08/03/2018
ms.locfileid: "39508327"
---
# <a name="install-command-nuget-cli"></a><span data-ttu-id="cb855-103">Comando install (CLI de NuGet)</span><span class="sxs-lookup"><span data-stu-id="cb855-103">install command (NuGet CLI)</span></span>

<span data-ttu-id="cb855-104">**Se aplica a:** consumo de paquetes &bullet; **versiones compatibles:** todas</span><span class="sxs-lookup"><span data-stu-id="cb855-104">**Applies to:** package consumption &bullet; **Supported versions:** all</span></span>

<span data-ttu-id="cb855-105">Descarga e instala un paquete en un proyecto, de forma predeterminada a la carpeta actual, con los orígenes de paquete especificado.</span><span class="sxs-lookup"><span data-stu-id="cb855-105">Downloads and installs a package into a project, defaulting to the current folder, using specified package sources.</span></span>

> [!Tip]
> <span data-ttu-id="cb855-106">Para descargar un paquete directamente fuera del contexto de un proyecto, visite la página del paquete en [nuget.org](https://www.nuget.org) y seleccione el **descargar** vínculo.</span><span class="sxs-lookup"><span data-stu-id="cb855-106">To download a package directly outside the context of a project, visit the package's page on [nuget.org](https://www.nuget.org) and select the **Download** link.</span></span>

<span data-ttu-id="cb855-107">Si no se especifica ningún origen, aquellos que aparecen en el archivo de configuración global, `%appdata%\NuGet\NuGet.Config` (Windows) o `~/.nuget/NuGet/NuGet.Config` (Mac/Linux), se usan.</span><span class="sxs-lookup"><span data-stu-id="cb855-107">If no sources are specified, those listed in the global configuration file, `%appdata%\NuGet\NuGet.Config` (Windows) or `~/.nuget/NuGet/NuGet.Config` (Mac/Linux), are used.</span></span> <span data-ttu-id="cb855-108">Consulte [del comportamiento de configuración de NuGet](../consume-packages/configuring-nuget-behavior.md) para obtener más detalles.</span><span class="sxs-lookup"><span data-stu-id="cb855-108">See [Configuring NuGet behavior](../consume-packages/configuring-nuget-behavior.md) for additional details.</span></span>

<span data-ttu-id="cb855-109">Si se especifica ningún paquete específico, `install` instala todos los paquetes enumerados en el proyecto `packages.config` archivo, lo que parece [ `restore` ](cli-ref-restore.md).</span><span class="sxs-lookup"><span data-stu-id="cb855-109">If no specific packages are specified, `install` installs all packages listed in the project's `packages.config` file, making it similar to [`restore`](cli-ref-restore.md).</span></span>

<span data-ttu-id="cb855-110">El `install` comando no modifica un archivo de proyecto o `packages.config`; de este modo es similar a `restore` en que solo agrega paquetes en el disco pero no cambia las dependencias de un proyecto.</span><span class="sxs-lookup"><span data-stu-id="cb855-110">The `install` command does not modify a project file or `packages.config`; in this way it's similar to `restore` in that it only adds packages to disk but does not change a project's dependencies.</span></span>

<span data-ttu-id="cb855-111">Para agregar una dependencia, agregar un paquete mediante el Administrador de paquetes de interfaz de usuario o la consola en Visual Studio o modificar `packages.config` y, a continuación, ejecute cualquiera `install` o `restore`.</span><span class="sxs-lookup"><span data-stu-id="cb855-111">To add a dependency, either add a package through the Package Manager UI or Console in Visual Studio, or modify `packages.config` and then run either `install` or `restore`.</span></span>

## <a name="usage"></a><span data-ttu-id="cb855-112">Uso</span><span class="sxs-lookup"><span data-stu-id="cb855-112">Usage</span></span>

```cli
nuget install <packageID | configFilePath> [options]
```

<span data-ttu-id="cb855-113">donde `<packageID>` nombra el paquete que desea instalar (con la versión más reciente), o `<configFilePath>` identifica el `packages.config` archivo que enumera los paquetes que se va a instalar.</span><span class="sxs-lookup"><span data-stu-id="cb855-113">where `<packageID>` names the package to install (using the latest version), or `<configFilePath>` identifies the `packages.config` file that lists the packages to install.</span></span> <span data-ttu-id="cb855-114">Puede indicar una versión específica con el `-Version` opción.</span><span class="sxs-lookup"><span data-stu-id="cb855-114">You can indicate a specific version with the `-Version` option.</span></span>

## <a name="options"></a><span data-ttu-id="cb855-115">Opciones</span><span class="sxs-lookup"><span data-stu-id="cb855-115">Options</span></span>

| <span data-ttu-id="cb855-116">Opción</span><span class="sxs-lookup"><span data-stu-id="cb855-116">Option</span></span> | <span data-ttu-id="cb855-117">Descripción</span><span class="sxs-lookup"><span data-stu-id="cb855-117">Description</span></span> |
| --- | --- |
| <span data-ttu-id="cb855-118">ConfigFile</span><span class="sxs-lookup"><span data-stu-id="cb855-118">ConfigFile</span></span> | <span data-ttu-id="cb855-119">El archivo de configuración para aplicar.</span><span class="sxs-lookup"><span data-stu-id="cb855-119">The NuGet configuration file to apply.</span></span> <span data-ttu-id="cb855-120">Si no se especifica, `%AppData%\NuGet\NuGet.Config` (Windows) o `~/.nuget/NuGet/NuGet.Config` (Mac/Linux) se utiliza.</span><span class="sxs-lookup"><span data-stu-id="cb855-120">If not specified, `%AppData%\NuGet\NuGet.Config` (Windows) or `~/.nuget/NuGet/NuGet.Config` (Mac/Linux) is used.</span></span>|
| <span data-ttu-id="cb855-121">DependencyVersion</span><span class="sxs-lookup"><span data-stu-id="cb855-121">DependencyVersion</span></span> | <span data-ttu-id="cb855-122">*(4.4 +)*  La versión de los paquetes de dependencia para usar, que puede ser uno de los siguientes:</span><span class="sxs-lookup"><span data-stu-id="cb855-122">*(4.4+)* The version of the dependency packages to use, which can be one of the following:</span></span><br/><ul><li><span data-ttu-id="cb855-123">*Menor* (valor predeterminado): la versión más antigua</span><span class="sxs-lookup"><span data-stu-id="cb855-123">*Lowest* (default): the lowest version</span></span></li><li><span data-ttu-id="cb855-124">*HighestPatch*: la versión con la revisión menor principal, secundaria menor, más alta</span><span class="sxs-lookup"><span data-stu-id="cb855-124">*HighestPatch*: the version with the lowest major, lowest minor, highest patch</span></span></li><li><span data-ttu-id="cb855-125">*HighestMinor*: la versión con el menor principales, revisiones secundarias y de mayor más alto</span><span class="sxs-lookup"><span data-stu-id="cb855-125">*HighestMinor*: the version with the lowest major, highest minor, highest patch</span></span></li><li><span data-ttu-id="cb855-126">*Mayor*: la versión más alta</span><span class="sxs-lookup"><span data-stu-id="cb855-126">*Highest*: the highest version</span></span></li></ul> |
| <span data-ttu-id="cb855-127">DisableParallelProcessing</span><span class="sxs-lookup"><span data-stu-id="cb855-127">DisableParallelProcessing</span></span> | <span data-ttu-id="cb855-128">Deshabilita la instalación de varios paquetes en paralelo.</span><span class="sxs-lookup"><span data-stu-id="cb855-128">Disables installing multiple packages in parallel.</span></span> |
| <span data-ttu-id="cb855-129">ExcludeVersion</span><span class="sxs-lookup"><span data-stu-id="cb855-129">ExcludeVersion</span></span> | <span data-ttu-id="cb855-130">Instala el paquete en una carpeta denominada con solo el nombre del paquete y no el número de versión.</span><span class="sxs-lookup"><span data-stu-id="cb855-130">Installs the package to a folder named with only the package name and not the version number.</span></span> |
| <span data-ttu-id="cb855-131">FallbackSource</span><span class="sxs-lookup"><span data-stu-id="cb855-131">FallbackSource</span></span> | <span data-ttu-id="cb855-132">*(3.2 y versiones posteriores)*  Una lista de orígenes de paquetes para utilizar como de reservas en caso de que el paquete no se encuentra en el servidor principal o el origen predeterminado.</span><span class="sxs-lookup"><span data-stu-id="cb855-132">*(3.2+)* A list of package sources to use as fallbacks in case the package isn't found in the primary or default source.</span></span> |
| <span data-ttu-id="cb855-133">ForceEnglishOutput</span><span class="sxs-lookup"><span data-stu-id="cb855-133">ForceEnglishOutput</span></span> | <span data-ttu-id="cb855-134">*(3.5 y versiones posteriores)*  Fuerza nuget.exe se ejecute con una referencia cultural invariable, en inglés.</span><span class="sxs-lookup"><span data-stu-id="cb855-134">*(3.5+)* Forces nuget.exe to run using an invariant, English-based culture.</span></span> |
| <span data-ttu-id="cb855-135">Framework</span><span class="sxs-lookup"><span data-stu-id="cb855-135">Framework</span></span> | <span data-ttu-id="cb855-136">*(4.4 +)*  Se usa para seleccionar las dependencias de .NET framework de destino.</span><span class="sxs-lookup"><span data-stu-id="cb855-136">*(4.4+)* Target framework used for selecting dependencies.</span></span> <span data-ttu-id="cb855-137">El valor predeterminado es "Any" Si no se especifica.</span><span class="sxs-lookup"><span data-stu-id="cb855-137">Defaults to 'Any' if not specified.</span></span> |
| <span data-ttu-id="cb855-138">Ayuda</span><span class="sxs-lookup"><span data-stu-id="cb855-138">Help</span></span> | <span data-ttu-id="cb855-139">Muestra información de ayuda para el comando.</span><span class="sxs-lookup"><span data-stu-id="cb855-139">Displays help information for the command.</span></span> |
| <span data-ttu-id="cb855-140">NoCache</span><span class="sxs-lookup"><span data-stu-id="cb855-140">NoCache</span></span> | <span data-ttu-id="cb855-141">Impide que NuGet utilice paquetes almacenados en caché.</span><span class="sxs-lookup"><span data-stu-id="cb855-141">Prevents NuGet from using cached packages.</span></span> <span data-ttu-id="cb855-142">Consulte [administración de paquetes globales y carpetas de caché](../consume-packages/managing-the-global-packages-and-cache-folders.md).</span><span class="sxs-lookup"><span data-stu-id="cb855-142">See [Managing the global packages and cache folders](../consume-packages/managing-the-global-packages-and-cache-folders.md).</span></span> |
| <span data-ttu-id="cb855-143">No interactivo</span><span class="sxs-lookup"><span data-stu-id="cb855-143">NonInteractive</span></span> | <span data-ttu-id="cb855-144">Suprime los mensajes para confirmaciones o intervención del usuario.</span><span class="sxs-lookup"><span data-stu-id="cb855-144">Suppresses prompts for user input or confirmations.</span></span> |
| <span data-ttu-id="cb855-145">OutputDirectory</span><span class="sxs-lookup"><span data-stu-id="cb855-145">OutputDirectory</span></span> | <span data-ttu-id="cb855-146">Especifica la carpeta donde se instalan los paquetes.</span><span class="sxs-lookup"><span data-stu-id="cb855-146">Specifies the folder in which packages are installed.</span></span> <span data-ttu-id="cb855-147">Si se especifica ninguna carpeta, se usa la carpeta actual.</span><span class="sxs-lookup"><span data-stu-id="cb855-147">If no folder is specified, the current folder is used.</span></span> |
| <span data-ttu-id="cb855-148">PackageSaveMode</span><span class="sxs-lookup"><span data-stu-id="cb855-148">PackageSaveMode</span></span> | <span data-ttu-id="cb855-149">Especifica los tipos de archivos que se va a guardar tras la instalación del paquete: uno de `nuspec`, `nupkg`, o `nuspec;nupkg`.</span><span class="sxs-lookup"><span data-stu-id="cb855-149">Specifies the types of files to save after package installation: one of `nuspec`, `nupkg`, or `nuspec;nupkg`.</span></span> |
| <span data-ttu-id="cb855-150">Versión preliminar</span><span class="sxs-lookup"><span data-stu-id="cb855-150">PreRelease</span></span> | <span data-ttu-id="cb855-151">Permite que los paquetes preliminares instalarse.</span><span class="sxs-lookup"><span data-stu-id="cb855-151">Allows prerelease packages to be installed.</span></span> <span data-ttu-id="cb855-152">Esta marca no es necesaria al restaurar los paquetes con `packages.config`.</span><span class="sxs-lookup"><span data-stu-id="cb855-152">This flag is not required when restoring packages with `packages.config`.</span></span> |
| <span data-ttu-id="cb855-153">RequireConsent</span><span class="sxs-lookup"><span data-stu-id="cb855-153">RequireConsent</span></span> | <span data-ttu-id="cb855-154">Comprueba que la restauración de paquetes está habilitada antes de descargar e instalar los paquetes.</span><span class="sxs-lookup"><span data-stu-id="cb855-154">Verifies that restoring packages is enabled before downloading and installing the packages.</span></span> <span data-ttu-id="cb855-155">Para obtener más información, consulte [la restauración del paquete](../consume-packages/package-restore.md).</span><span class="sxs-lookup"><span data-stu-id="cb855-155">For details, see [Package Restore](../consume-packages/package-restore.md).</span></span> |
| <span data-ttu-id="cb855-156">SolutionDirectory</span><span class="sxs-lookup"><span data-stu-id="cb855-156">SolutionDirectory</span></span> | <span data-ttu-id="cb855-157">Especifica la carpeta raíz de la solución para que se va a restaurar los paquetes.</span><span class="sxs-lookup"><span data-stu-id="cb855-157">Specifies root folder of the solution for which to restore packages.</span></span> |
| <span data-ttu-id="cb855-158">Origen</span><span class="sxs-lookup"><span data-stu-id="cb855-158">Source</span></span> | <span data-ttu-id="cb855-159">Especifica la lista de orígenes de paquetes (como las direcciones URL) para usar.</span><span class="sxs-lookup"><span data-stu-id="cb855-159">Specifies the list of package sources (as URLs) to use.</span></span> <span data-ttu-id="cb855-160">Si se omite, el comando usa los orígenes proporcionados en los archivos de configuración, consulte [del comportamiento de configuración de NuGet](../consume-packages/configuring-nuget-behavior.md).</span><span class="sxs-lookup"><span data-stu-id="cb855-160">If omitted, the command uses the sources provided in configuration files, see [Configuring NuGet behavior](../consume-packages/configuring-nuget-behavior.md).</span></span> |
| <span data-ttu-id="cb855-161">Nivel de detalle</span><span class="sxs-lookup"><span data-stu-id="cb855-161">Verbosity</span></span> | <span data-ttu-id="cb855-162">Especifica la cantidad de detalle que se muestra en la salida: *normal*, *quiet*, *detallada*.</span><span class="sxs-lookup"><span data-stu-id="cb855-162">Specifies the amount of detail displayed in the output: *normal*, *quiet*, *detailed*.</span></span> |
| <span data-ttu-id="cb855-163">Versión</span><span class="sxs-lookup"><span data-stu-id="cb855-163">Version</span></span> | <span data-ttu-id="cb855-164">Especifica la versión del paquete para instalar.</span><span class="sxs-lookup"><span data-stu-id="cb855-164">Specifies the version of the package to install.</span></span> |

<span data-ttu-id="cb855-165">Consulte también [variables de entorno](cli-ref-environment-variables.md)</span><span class="sxs-lookup"><span data-stu-id="cb855-165">Also see [Environment variables](cli-ref-environment-variables.md)</span></span>

## <a name="examples"></a><span data-ttu-id="cb855-166">Ejemplos</span><span class="sxs-lookup"><span data-stu-id="cb855-166">Examples</span></span>

```cli
nuget install elmah

nuget install packages.config

nuget install ninject -OutputDirectory c:\proj
```
